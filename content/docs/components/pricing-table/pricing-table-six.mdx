---
title: Pricing Table Six
description: The Pricing Table Six component provides a premium pricing table design with image backgrounds and pill-shaped badges. Features split-card layout with dynamic background images in the top half and pricing details in the bottom half.
---

## Default Theme

<Tabs items={['Preview', 'Code']} className="bg-transparent border-none">
  <Tab value="Preview" className="border-none bg-transparent p-0 mt-3">
    <PreviewComponents registryName="pricing-table-six">
      <PricingTableSixDemo />
    </PreviewComponents>
  </Tab>

  <Tab value="Code" className="mt-3">
    <include cwd lang="tsx" meta='title="src/components/pricing-table-six-demo.tsx"'>src/components/pricing-table-six-demo.tsx</include>
  </Tab>
</Tabs>

## Installation

<Tabs items={['shadcn', 'billingSDK']} defaultValue="shadcn" className="bg-transparent border-none">
  <Tab value="shadcn" className="border-none bg-transparent p-0 mt-3">
    <Tabs items={['npx', 'pnpm', 'yarn']} defaultValue="npx" groupId="installation-tabs">
      <Tab value="npx">
        ```bash
        npx shadcn@latest add https://billingsdk.com/r/pricing-table-six.json
        ```
      </Tab>
      <Tab value="pnpm">
        ```bash
        pnpm dlx shadcn@latest add https://billingsdk.com/r/pricing-table-six.json
        ```
      </Tab>
      <Tab value="yarn">
        ```bash
        npx shadcn@latest add https://billingsdk.com/r/pricing-table-six.json
        ```
      </Tab>
    </Tabs>
  </Tab>
  <Tab value="billingSDK" className="border-none bg-transparent p-0 mt-3">
    <Tabs items={['npx', 'pnpm', 'yarn']} defaultValue="npx" groupId="billingsdk-cli-tabs">
      <Tab value="npx">
        ```bash
        npx @billingsdk/cli add pricing-table-six
        ```
      </Tab>
      <Tab value="pnpm">
        ```bash
        pnpm dlx @billingsdk/cli add pricing-table-six
        ```
      </Tab>
      <Tab value="yarn">
        ```bash
        npx @billingsdk/cli add pricing-table-six
        ```
      </Tab>
    </Tabs>
  </Tab>
</Tabs>

## Usage

```tsx
import { PricingTableSix } from "@/components/billingsdk/pricing-table-six";
import { plans } from "@/lib/billingsdk-config";

<PricingTableSix 
  plans={plans}
  title="Premium Pricing"
  description="Experience our premium pricing cards with stunning visual designs"
  onPlanSelect={(planId) => console.log('Selected plan:', planId)}
  size="medium" // small, medium, large
  theme="minimal" // minimal or classic
  backgroundImages={{
    'starter': 'url-to-starter-image',
    'pro': 'url-to-pro-image', 
    'enterprise': 'url-to-enterprise-image'
  }}
/>
```

## Props

| Prop | Type | Description |
|------|------|-------------|
| `plans` | `Plan[]` | Array of pricing plans (see [Plan interface](/docs/interfaces#plan-interface)) |
| `title` | `string` | Main title for the pricing section |
| `description` | `string` | Subtitle description |
| `onPlanSelect` | `(planId: string) => void` | Callback when a plan is selected |
| `size` | `"small" \| "medium" \| "large"` | Size variant of the pricing table (default: "medium") |
| `theme` | `"minimal" \| "classic"` | Theme variant (default: "minimal") |
| `backgroundImages` | `{ [key: string]: string }` | Custom background images for each plan (optional) |
| `className` | `string` | Additional CSS classes |

## Features

### 🖼️ Image Integration
- **Split Layout**: Image container in top half, pricing details in bottom half
- **Dynamic Backgrounds**: Customizable background images per plan via `backgroundImages` prop
- **Animated Elements**: Floating abstract shapes with smooth animations
- **Default Images**: Includes colorful fluid/galaxy-like swirl patterns as fallbacks

### 🏷️ Pill-Shaped Badges
- **Plan Type Badge**: Positioned in top-left (e.g., "Pro", "Starter")
- **"Most Popular" Badge**: Positioned in top-right for highlighted plans
- **Styling**: Black background with white text and subtle borders

### 📱 Responsive Design
- **Mobile-First**: Adapts to different screen sizes
- **Rounded Corners**: Soft radius for modern appearance
- **Dark Borders**: Subtle shadows for visual separation
- **Optimized Spacing**: Plenty of padding and whitespace

## Theming

The pricing table component is styled using the `shadcn/ui` library. You can customize the colors and fonts by overriding the CSS variables. You can also get the theme from the [Theming](/docs/theming) page.

## Example
<include cwd lang="tsx" meta='title="src/components/pricing-table-six-demo.tsx"'>src/components/pricing-table-six-demo.tsx</include>

### Credits
- Created by [@Sarthak_Wade](https://x.com/Sarthak_Wade)